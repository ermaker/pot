metricbeat.config.modules:
  path: ${path.config}/modules.d/*.yml
  reload.enabled: false

processors:
- add_cloud_metadata:

output.elasticsearch.hosts: '${ES_HOSTS:http://localhost:9200}'

setup.dashboards.enabled: '${BEAT_DASHBOARDS:false}'
setup.kibana.host: '${KIBANA_HOST:http://localhost:5601}'
